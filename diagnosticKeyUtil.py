
# This Python script was used to generate some of the code in "diagnosticsKeyCode.keylayout".

for i in range(500):
	lineToPrint = "<key code=" + '"' + str(i) + '"' + " output=" + '"' + str(i) + '"' + "/>"
	print("\t\t\t" + lineToPrint)
